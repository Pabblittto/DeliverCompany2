@page
@model DazyBanychMVC.Pages.Warehouses.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Warehouses</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Warehous[0].department)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Warehous[0].Street)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Warehous[0].HouseNumber)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Warehous) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.department.ManagerTelNo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Street)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HouseNumber)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
