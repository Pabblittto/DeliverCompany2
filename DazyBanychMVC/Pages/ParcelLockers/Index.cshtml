@page
@model DazyBanychMVC.Pages.ParcelLockers.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Parcel Lockers</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ParcelLocker[0].street)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ParcelLocker) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.street.StreetName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
